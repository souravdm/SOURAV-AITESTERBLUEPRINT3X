# Framework-level settings (not env-specific)
report.output.dir=target/extent-report
report.archive.dir=target/extent-report/archive
log.output.dir=target/logs
response.time.sla.ms=5000
retry.count=1
parallel.thread.count=4
